// Global routes
const HOME = "/";
const JOIN = "/join";

// API routes
const API = "/api";
const ME = "/me";

const routes = {
  // Global routes
  home: HOME,
  join: JOIN,
  // Api routes
  api: API,
  me: ME,
};

export default routes;
