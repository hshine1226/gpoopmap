// Global routes
const HOME = "/";
const JOIN = "/join";
const LOGIN = "/login";
const LOGOUT = "/logout";

// API routes
const API = "/api";
const ME = "/me";

const routes = {
  // Global routes
  home: HOME,
  join: JOIN,
  login: LOGIN,
  logout: LOGOUT,
  // Api routes
  api: API,
  me: ME,
};

export default routes;
